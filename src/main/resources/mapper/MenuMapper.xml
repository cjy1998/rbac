<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jy.rbac.mapper.MenuMapper">
    <sql id="cols">
        menuId,
        menuName,
        parentId,
        orderNum,
        path,
        component,
        query,
        isFrame,
        isCache,
        menuType,
        visible,
        status,
        perms,
        icon,
        createBy,
        createTime,
        updateBy,
        updateTime,
        remark
    </sql>
    <insert id="add" useGeneratedKeys="true" keyProperty="menuId">
        insert into sys_menu
        (menuName,
        parentId,
        orderNum,
        path,
        component,
        query,
        isFrame,
        isCache,
        menuType,
        visible,
        status,
        perms,
        icon,
        createBy,
        createTime,
        updateBy,
        updateTime,
        remark) values
        (
        #{menuName},
        #{parentId},
        #{orderNum},
        #{path},
        #{component},
        #{query},
        #{isFrame},
        #{isCache},
        #{menuType},
        #{visible},
        #{status},
        #{perms},
        #{icon},
        #{createBy},
        #{createTime},
        #{updateBy},
        #{updateTime},
        #{remark}
        )
    </insert>
    <update id="edit">
        UPDATE
        sys_menu
        <set>
            <if test="menuName != null and menuName != ''">
                menuName = #{menuName},
            </if>
            <if test="parentId != null">
                parentId = #{parentId},
            </if>
            <if test="orderNum != null">
                orderNum = #{orderNum},
            </if>
            <if test="path != null and path != ''">
                path = #{path},
            </if>
            <if test="component != null">
                component = #{component},
            </if>
            <if test="query != null">
                query = #{query},
            </if>
            <if test="isFrame != null and isFrame != ''">
                isFrame = #{isFrame},
            </if>
            <if test="isCache != null and isCache != ''">
                isCache = #{isCache},
            </if>
            <if test="menuType != null and menuType != ''">
                menuType = #{menuType},
            </if>
            <if test="visible != null and visible != ''">
                visible = #{visible},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
            <if test="perms != null and perms != ''">
                perms = #{perms},
            </if>
            <if test="icon != null">
                icon = #{icon},
            </if>
            <if test="remark != null">
                remark = #{remark},
            </if>
            <if test="updateBy != null and updateBy != ''">
                updateBy = #{updateBy},
            </if>
            <if test="updateTime != null">
                updateTime = #{updateTime},
            </if>
        </set>
        where menuId = #{menuId}
    </update>
    <select id="getList" resultType="com.jy.rbac.pojo.entity.Menu">
        select <include refid="cols" /> from sys_menu
        <where>
            <if test="menuName != null and menuName != ''">
                and menuName like concat('%', #{menuName}, '%')
            </if>
            <if test="status != null and status != ''">
                and status = #{status}
            </if>
        </where>
        order by orderNum
    </select>
    <select id="getMenuById" resultType="com.jy.rbac.pojo.entity.Menu">
        select <include refid="cols" /> from sys_menu where menuId = #{menuId}
    </select>
    <select id="getAll" resultType="com.jy.rbac.pojo.entity.Menu">
        select <include refid="cols" /> from sys_menu order by orderNum
    </select>

    <select id="countByIds" resultType="long">
        select count(*) from sys_menu where menuId in
        <foreach collection="menuIds" item="menuId" open="(" separator="," close=")">
            #{menuId}
        </foreach>
    </select>

</mapper>
