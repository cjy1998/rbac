<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.jy.rbac.mapper.RoleMapper">

    <sql id="cols">
        roleId,
        createBy,
        createTime,
        dataScope,
        delFlag,
        deptCheckStrictly,
        menuCheckStrictly,
        remark,
        roleKey,
        roleName,
        roleSort,
        status,
        updateBy,
        updateTime
    </sql>
    <insert id="add">
        INSERT
        INTO
        sys_role (
        createBy,
        createTime,
        dataScope,
        deptCheckStrictly,
        menuCheckStrictly,
        remark,
        roleKey,
        roleName,
        roleSort,
        status,
        updateBy,
        updateTime)
        VALUES(
        #{createBy},
        #{createTime},
        #{dataScope},
        #{deptCheckStrictly},
        #{menuCheckStrictly},
        #{remark},
        #{roleKey},
        #{roleName},
        #{roleSort},
        #{status},
        #{updateBy},
        #{updateTime})
    </insert>
    <update id="edit">
        UPDATE
        sys_role
        <set>
            <if test="roleName != null and roleName != ''">
                roleName = #{roleName},
            </if>
            <if test="roleKey != null and roleKey != ''">
                roleKey = #{roleKey},
            </if>
            <if test="dataScope != null and dataScope != ''">
                dataScope = #{dataScope},
            </if>
            <if test="deptCheckStrictly != null and deptCheckStrictly != ''">
                deptCheckStrictly = #{deptCheckStrictly},
            </if>
            <if test="menuCheckStrictly != null and menuCheckStrictly != ''">
                menuCheckStrictly = #{menuCheckStrictly},
            </if>
            <if test="remark != null and remark != ''">
                remark = #{remark},
            </if>
            <if test="roleSort != null and roleSort != ''">
                roleSort = #{roleSort},
            </if>
            <if test="status != null and status != ''">
                status = #{status},
            </if>
            <if test="updateBy != null and updateBy != ''">
                updateBy = #{updateBy},
            </if>
            <if test="updateTime != null">
                updateTime = #{updateTime}
            </if>
        </set>
        where delFlag != '1' and roleId = #{roleId}
    </update>
    <select id="getList" resultType="com.jy.rbac.pojo.entity.Role">
        select <include refid="cols" /> from sys_role
        <where>
            delFlag != '1'
            <if test="roleName !=null and roleName != ''">
              and  roleName like concat('%',#{roleName},'%')
            </if>
            <if test="roleKey !=null and roleKey != ''">
                and roleKey = #{roleKey}
            </if>
        </where>
    </select>
    <select id="getRoleById" resultType="com.jy.rbac.pojo.entity.Role">
        select <include refid="cols" /> from sys_role where delFlag != '1' and roleId = #{roleId}
    </select>

    <select id="countByIds" resultType="long">
        select count(*) from sys_role
        where delFlag != '1' and roleId in
        <foreach collection="roleIds" item="roleId" open="(" separator="," close=")">
            #{roleId}
        </foreach>
    </select>

</mapper>
